# Raw data

Đặt tại đây các file dữ liệu **thô** sau khi chạy crawler:

- `laptop_tgdd_*.csv` — crawl từ Thế Giới Di Động
- `laptop_fpt_*.csv` — crawl từ FPT Shop (nếu có)
- `laptop_phongvu_*.csv` — crawl từ Phong Vũ (nếu có)

**Không** chỉnh sửa tay file trong folder này; dùng làm bản gốc để so sánh trước/sau làm sạch.
