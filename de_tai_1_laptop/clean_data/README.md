# Clean data

Đặt tại đây file dữ liệu **đã làm sạch và chuẩn hóa** (sau bước làm sạch trong notebook hoặc script):

- `laptop_clean.csv` — dataset đã xử lý, dùng cho EDA, feature engineering và trực quan hóa.

Kích thước tối đa theo yêu cầu GV (ví dụ 20 MB).
